import InstallationType from '@/components/admin/installationType/InstallationType';
import React from 'react';

const page = () => {
    return (
        <div>
           <InstallationType/>
        </div>
    );
};

export default page;